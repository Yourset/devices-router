# Devices Router v0.1.30

## 稳定重连修复

- 延续 `v0.1.29` 的键盘专用稳定模式，鼠标能力继续保持停用。
- 修复副电脑退出、更新或断线后，主电脑错误占用旧 TCP 会话的问题。
- 主电脑现在会在读到 TCP EOF 时立即结束旧会话、释放本机键盘并接受副电脑重连。
- 新增回归测试，锁定零字节读取必须结束当前连接。

## 验证

- Rust：64 个测试通过。
- Python：33 个测试通过。
- Rust Clippy、TypeScript 与 Vite 构建通过。
