# Devices Router v0.1.15

- Reverted the main remote-input path from pre-translated Unicode text to real virtual-key events so the remote PC's own IME can handle composition.
- Switched remote key injection to virtual-key `SendInput`, improving system shortcuts such as Alt+Tab and console input compatibility.
