# Devices Router v0.1.17

## Changes

- Fixed overview and settings layout overlap that could block buttons, including the administrator restart prompt.
- Changed the main content area to scroll normally instead of clipping taller panels.
- Kept the log panel from covering controls above it.

## Notes

This version intentionally bumps the package version so secondary PCs can detect and install the layout fix through LAN update.
