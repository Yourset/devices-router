# Devices Router v0.1.31

## 恢复稳定的键盘自动跟随

- 恢复 `v0.1.22` 已验证的鼠标活动跟随方式：副电脑鼠标活动时键盘切到副电脑，主电脑鼠标活动时键盘切回主电脑。
- 跨电脑鼠标移动、点击、滚轮转发和鼠标拦截继续保持停用。
- 鼠标活动只作为键盘目标信号，不会移动、锁定或接管任何一台电脑的鼠标。
- 保留 `Ctrl+Alt+1/2` 手动切换和 `Ctrl+Alt+Esc` 紧急释放。
- 保留 `v0.1.30` 的断线重连修复。

## 验证

- Rust、Python、Clippy、TypeScript 和 Vite 构建通过。
